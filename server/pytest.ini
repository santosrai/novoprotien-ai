[pytest]
testpaths = __tests__
asyncio_mode = auto
addopts = -v --tb=short
markers =
    integration: marks tests requiring a running server or external API
    slow: marks tests that take more than 10 seconds
